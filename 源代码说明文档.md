# 源代码说明文档

校园体育器材出入库管理系统

---

## 一、项目概述

本系统是一个基于 Python 和 PyQt5 开发的校园体育器材出入库管理系统，主要用于管理篮球、排球、羽毛球拍、乒乓球拍、网球拍等五类常用体育器材的日常出入库业务。

## 二、项目文件结构

```
sports_equipment_system/
├── config.py           # 系统配置模块
├── database.py         # 数据库连接与初始化
├── logger.py           # 日志管理模块
├── auth.py             # 用户认证与权限管理
├── equipment.py        # 器材信息管理
├── inout.py            # 出入库管理
├── inventory.py        # 库存管理
├── report.py           # 报表统计
├── ui.py               # PyQt5用户界面
├── main.py             # 系统入口
└── sports_equipment.db # SQLite数据库文件（运行时自动创建）
```

---

## 三、各模块详细说明

### 3.1 config.py - 配置模块

**功能**：系统配置集中管理

**主要配置项**：
- `APP_NAME` - 应用名称
- `APP_VERSION` - 应用版本
- `USE_SQLITE` - 是否使用SQLite数据库
- `SQLITE_DB_PATH` - SQLite数据库文件路径
- `DB_CONFIG` - MySQL数据库配置
- `LOG_CONFIG` - 日志配置（级别、格式、文件路径、轮转策略）
- `INVENTORY_WARNING_CONFIG` - 库存预警阈值配置
- `EQUIPMENT_TYPES` - 器材类型定义
- `USER_ROLES` - 用户角色权限配置

**关键代码片段**：
```python
APP_NAME = "校园体育器材出入库管理系统"
APP_VERSION = "1.0.0"

USE_SQLITE = True
SQLITE_DB_PATH = 'sports_equipment.db'

INVENTORY_WARNING_CONFIG = {
    'basketball': 10,
    'volleyball': 10,
    'badminton_racket': 20,
    'pingpong_racket': 20,
    'tennis_racket': 10
}

EQUIPMENT_TYPES = [
    ('basketball', '篮球'),
    ('volleyball', '排球'),
    ('badminton_racket', '羽毛球拍'),
    ('pingpong_racket', '乒乓球拍'),
    ('tennis_racket', '网球拍')
]
```

---

### 3.2 database.py - 数据库模块

**功能**：数据库连接管理与初始化

**主要特性**：
- 单例模式数据库连接管理
- 支持SQLite和MySQL切换
- 自动初始化数据库表结构
- 创建默认管理员账户（admin/admin123）
- 数据库备份与恢复

**关键类**：`Database`

**主要方法**：
- `execute(sql, params)` - 执行SQL语句
- `fetchall(sql, params)` - 查询多条记录
- `fetchone(sql, params)` - 查询单条记录
- `backup_database()` - 备份数据库
- `close()` - 关闭数据库连接

**辅助函数**：
- `init_database()` - 初始化数据库表
- `create_default_admin(db)` - 创建默认管理员账户
- `backup_database()` - 数据库备份
- `cleanup_old_backups()` - 清理旧备份文件
- `get_db()` - 获取数据库实例

---

### 3.3 logger.py - 日志模块

**功能**：日志记录与管理

**主要特性**：
- 分模块日志记录
- 日志文件轮转（10MB/文件，保留5份）
- 操作日志数据库记录
- 日志级别管理

**关键类**：`Logger`

**主要方法**：
- `get_logger(name)` - 获取日志实例

**辅助函数**：
- `log_operation(user_id, username, op_type, module, desc, ip)` - 记录操作日志到数据库
- `log_info(message, module)` - 记录信息日志
- `log_error(message, module)` - 记录错误日志
- `log_warning(message, module)` - 记录警告日志
- `get_operation_logs(user_id, op_type, start_time, end_time, limit)` - 查询操作日志

---

### 3.4 auth.py - 认证模块

**功能**：用户认证与权限管理

**主要类**：

#### UserAuth
- `hash_password(password)` - 密码哈希（SHA256）
- `verify_password(password, password_hash)` - 密码验证
- `login(username, password)` - 用户登录
- `logout(user_id, username)` - 用户登出
- `register(username, password, real_name, role, email, phone)` - 用户注册
- `change_password(user_id, old_password, new_password)` - 修改密码
- `reset_password(user_id, new_password, admin_id)` - 重置密码

#### Permission
- `check_permission(role, permission)` - 权限检查
- `get_permissions(role)` - 获取权限列表

#### UserManager
- `get_user_by_id(user_id)` - 根据ID获取用户
- `get_user_by_username(username)` - 根据用户名获取用户
- `get_all_users(page, page_size, role, keyword)` - 分页获取用户列表
- `update_user(user_id, real_name, email, phone, role, status)` - 更新用户信息
- `delete_user(user_id, current_user_id)` - 删除用户

---

### 3.5 equipment.py - 器材管理模块

**功能**：器材基本信息管理

**主要类**：`EquipmentManager`

**主要方法**：
- `generate_equipment_code(equipment_type)` - 生成器材编号
- `add_equipment(equipment_code, name, type, specification, quantity, unit, purchase_date, supplier, purchase_price, remarks, created_by)` - 添加器材
- `update_equipment(equipment_id, name, specification, supplier, purchase_price, remarks, status)` - 更新器材信息
- `delete_equipment(equipment_id, user_id)` - 删除器材
- `get_equipment_by_id(equipment_id)` - 根据ID获取器材
- `get_equipment_by_code(equipment_code)` - 根据编号获取器材
- `get_all_equipments(page, page_size, equipment_type, keyword, status)` - 分页查询器材
- `get_equipment_statistics()` - 获取器材统计信息
- `update_quantity(equipment_id, quantity_change, operator_id, operator_name)` - 更新器材数量
- `get_specification_details_by_type(equipment_type)` - 获取某类型器材的规格详情（用于数据面板交互）

**器材编号规则**：
- 篮球：BB + 日期(YYYYMMDD) + 序号(4位)
- 排球：VB + 日期(YYYYMMDD) + 序号(4位)
- 羽毛球拍：BR + 日期(YYYYMMDD) + 序号(4位)
- 乒乓球拍：PR + 日期(YYYYMMDD) + 序号(4位)
- 网球拍：TR + 日期(YYYYMMDD) + 序号(4位)

---

### 3.6 inout.py - 出入库管理模块

**功能**：器材入库、出库操作管理

**主要类**：`InOutManager`

**主要方法**：
- `generate_record_no()` - 生成出入库记录编号
- `stock_in(equipment_id, quantity, purpose, remarks, operator_id, operator_name)` - 入库操作
- `stock_out(equipment_id, quantity, purpose, remarks, operator_id, operator_name)` - 出库操作
- `get_records(page, page_size, equipment_type, record_type, start_date, end_date, keyword)` - 分页查询出入库记录
- `get_record_by_id(record_id)` - 根据ID获取记录
- `get_record_statistics(start_date, end_date, equipment_type)` - 获取出入库统计
- `get_recent_records(limit)` - 获取最近记录
- `cancel_record(record_id, operator_id, operator_name, reason)` - 撤销记录

**记录编号规则**：`IO` + 日期(YYYYMMDD) + 时间(HHMMSS)

---

### 3.7 inventory.py - 库存管理模块

**功能**：库存监控与调整

**主要类**：`InventoryManager`

**主要方法**：
- `get_inventory_summary()` - 获取库存汇总
- `get_low_stock_warnings()` - 获取库存预警列表
- `check_and_create_warnings()` - 检测并创建预警记录
- `get_warnings(page, page_size, is_processed)` - 分页查询预警记录
- `process_warning(warning_id, operator_id)` - 处理预警
- `inventory_check(equipment_id, actual_quantity, check_type, checker_id, checker_name, remarks)` - 库存盘点
- `inventory_adjustment(equipment_id, new_quantity, reason, operator_id, operator_name)` - 库存调整

**预警规则**：
| 器材类型 | 预警阈值 |
|---------|---------|
| 篮球 | 10 |
| 排球 | 10 |
| 羽毛球拍 | 20 |
| 乒乓球拍 | 20 |
| 网球拍 | 10 |

---

### 3.8 report.py - 报表统计模块

**功能**：数据统计与报表生成

**主要类**：`ReportManager`

**主要方法**：
- `get_dashboard_data()` - 获取仪表盘数据
- `query_equipment(equipment_type, keyword, status)` - 查询器材
- `query_records(equipment_type, record_type, start_date, end_date, keyword)` - 查询记录
- `generate_daily_report(date)` - 生成日报
- `generate_monthly_report(year, month)` - 生成月报
- `export_to_csv(data, filename, headers)` - 导出CSV文件
- `get_statistics_by_date(start, end)` - 按日期统计

---

### 3.9 ui.py - 用户界面模块

**功能**：PyQt5图形用户界面

**主要特性**：
- 登录界面
- 主窗口布局（菜单栏、工具栏、标签页）
- 数据面板（仪表盘）- 支持点击数字查看规格详情
- 器材管理界面（CRUD操作）
- 出入库管理界面（入库登记、出库登记、记录查询）
- 库存管理界面（库存查询、预警、盘点、调整）
- 用户管理界面（管理员功能）
- 报表统计界面

**界面特点**：
- 标签页式导航
- 响应式布局设计（窗口最大化时行高一致）
- 数据面板数字支持点击交互
- 统一的表格样式和字体设置

**关键类**：`MainWindow`

**主要方法**：
- `__init__()` - 初始化主窗口
- `init_ui()` - 初始化界面
- `create_menubar()` - 创建菜单栏
- `create_toolbar()` - 创建工具栏
- `create_dashboard_tab()` - 创建数据面板标签页
- `create_equipment_tab()` - 创建器材管理标签页
- `create_inventory_tab()` - 创建库存管理标签页
- `create_inout_tab()` - 创建出入库记录标签页
- `show_specification_detail(type_code, type_name, detail_type)` - 显示规格详情对话框
- `refresh_dashboard_data()` - 刷新仪表盘数据

---

### 3.10 main.py - 系统入口

**功能**：系统启动入口

**主要流程**：
1. 初始化日志系统
2. 初始化数据库
3. 创建并显示登录界面
4. 登录成功后显示主窗口

**代码示例**：
```python
import sys
from PyQt5.QtWidgets import QApplication
from ui import LoginWindow, MainWindow
from database import init_database
from logger import Logger

if __name__ == "__main__":
    app = QApplication(sys.argv)
    
    logger = Logger.get_logger()
    logger.info("系统启动")
    
    init_database()
    
    login_window = LoginWindow()
    if login_window.exec_() == login_window.Accepted:
        main_window = MainWindow(login_window.current_user)
        main_window.showMaximized()
        sys.exit(app.exec_())
```

---

## 四、数据库表结构

### 4.1 users（用户表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 用户ID |
| username | VARCHAR(50) | UNIQUE NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（SHA256哈希） |
| real_name | VARCHAR(50) | | 真实姓名 |
| role | VARCHAR(20) | NOT NULL | 角色 |
| email | VARCHAR(100) | | 邮箱 |
| phone | VARCHAR(20) | | 联系电话 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| last_login | TIMESTAMP | | 最后登录时间 |
| status | INTEGER | DEFAULT 1 | 状态 |

### 4.2 equipment（器材表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 器材ID |
| equipment_code | VARCHAR(50) | UNIQUE NOT NULL | 器材编号 |
| name | VARCHAR(100) | NOT NULL | 器材名称 |
| type | VARCHAR(50) | NOT NULL | 器材类型 |
| specification | VARCHAR(200) | | 规格型号 |
| quantity | INTEGER | DEFAULT 0 | 当前库存 |
| unit | VARCHAR(20) | DEFAULT '个' | 单位 |
| purchase_date | DATE | | 采购日期 |
| supplier | VARCHAR(200) | | 供应商 |
| purchase_price | DECIMAL(10,2) | | 采购价格 |
| status | INTEGER | DEFAULT 1 | 状态 |
| remarks | TEXT | | 备注 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| created_by | INTEGER | FK | 创建人 |

### 4.3 in_out_record（出入库记录表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 记录ID |
| record_no | VARCHAR(50) | UNIQUE NOT NULL | 记录编号 |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| equipment_type | VARCHAR(50) | NOT NULL | 器材类型 |
| record_type | VARCHAR(20) | NOT NULL | 记录类型 |
| quantity | INTEGER | NOT NULL | 数量变化 |
| before_quantity | INTEGER | NOT NULL | 操作前库存 |
| after_quantity | INTEGER | NOT NULL | 操作后库存 |
| operator_id | INTEGER | FK NOT NULL | 操作人 |
| operator_name | VARCHAR(50) | | 操作人姓名 |
| operate_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 操作时间 |
| purpose | VARCHAR(500) | | 用途说明 |
| remarks | TEXT | | 备注 |

### 4.4 inventory_warning（库存预警表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 预警ID |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| equipment_type | VARCHAR(50) | NOT NULL | 器材类型 |
| current_quantity | INTEGER | NOT NULL | 当前库存 |
| warning_quantity | INTEGER | NOT NULL | 预警阈值 |
| warning_level | VARCHAR(20) | | 预警级别 |
| is_processed | INTEGER | DEFAULT 0 | 处理状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| processed_at | TIMESTAMP | | 处理时间 |
| processed_by | INTEGER | FK | 处理人 |

### 4.5 operation_log（操作日志表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 日志ID |
| user_id | INTEGER | FK | 用户ID |
| username | VARCHAR(50) | | 用户名 |
| operation_type | VARCHAR(50) | NOT NULL | 操作类型 |
| operation_module | VARCHAR(50) | | 操作模块 |
| operation_desc | TEXT | | 操作描述 |
| ip_address | VARCHAR(50) | | IP地址 |
| operate_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 操作时间 |
| status | INTEGER | DEFAULT 1 | 状态 |

### 4.6 inventory_check（库存盘点表）

| 字段名 | 类型 | 约束 | 说明 |
|-------|-----|------|-----|
| id | INTEGER | PRIMARY KEY | 盘点ID |
| check_no | VARCHAR(50) | UNIQUE NOT NULL | 盘点编号 |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| system_quantity | INTEGER | NOT NULL | 系统库存 |
| actual_quantity | INTEGER | NOT NULL | 实际库存 |
| difference | INTEGER | NOT NULL | 差异 |
| check_type | VARCHAR(20) | | 盘点类型 |
| checker_id | INTEGER | FK NOT NULL | 盘点人 |
| checker_name | VARCHAR(50) | | 盘点人姓名 |
| check_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 盘点时间 |
| remarks | TEXT | | 备注 |

---

## 五、关键功能说明

### 5.1 数据面板交互功能

数据面板中的数字支持点击交互：
- **点击资产总数**：弹出对话框，显示该类型器材各规格的资产总数明细（累计入库数量）
- **点击库存数量**：弹出对话框，显示该类型器材各规格的库存数量明细（当前库存）
- **鼠标悬停**：数字下方显示下划线，提示可点击

### 5.2 出入库事务处理

出入库操作采用事务机制：
1. 开启事务
2. 插入出入库记录
3. 更新器材库存
4. 自动提交（无异常）或回滚（异常时）

### 5.3 密码安全

- 密码使用SHA256哈希存储
- 不存储明文密码
- 登录时验证哈希值

---

## 六、系统依赖

```
PyQt5 >= 5.15.0
pymysql >= 1.0.0  (可选，用于MySQL)
```

---

## 七、运行环境

- Python 3.8+
- Windows / Linux / macOS
- 内存：最低 4GB
- 硬盘：最低 100MB 可用空间

---

## 八、启动方式

```bash
python main.py
```

---

## 九、默认账户

- 用户名：admin
- 密码：admin123

---

*源代码说明文档 - 校园体育器材出入库管理系统 V1.0.0*
