# 源代码说明文档

## 项目文件

```
xiaoyuanqicai/
├── config.py           # 系统配置
├── database.py         # 数据库
├── logger.py           # 日志
├── auth.py             # 用户认证
├── equipment.py        # 器材管理
├── inout.py            # 出入库
├── inventory.py        # 库存
├── report.py           # 报表
├── ui.py               # 界面
├── main.py             # 入口
└── sports_equipment.db # 数据库
```

## 模块说明

### config.py
系统配置，包括应用信息、数据库配置、日志配置、器材类型定义等。

### database.py
数据库连接管理，包含Database类，主要方法：
- `execute(sql, params)` - 执行SQL
- `fetchall(sql, params)` - 查询多条
- `fetchone(sql, params)` - 查询单条
- `init_database()` - 初始化数据库
- `get_db()` - 获取数据库实例

### logger.py
日志管理，主要方法：
- `get_logger(name)` - 获取日志实例
- `log_operation(...)` - 记录操作日志

### auth.py
用户认证模块，包含：
- **UserAuth**: 密码哈希、登录登出、注册、修改密码
- **Permission**: 权限检查
- **UserManager**: 用户管理

### equipment.py
器材管理，包含EquipmentManager类，主要方法：
- `generate_equipment_code(type)` - 生成编号
- `add(...)` - 添加器材
- `update(...)` - 更新器材
- `delete(...)` - 删除器材
- `get_by_id(id)` - 获取器材
- `get_all(...)` - 查询器材列表

器材编号规则：类型前缀 + 日期 + 序号

### inout.py
出入库管理，包含InOutManager类，主要方法：
- `stock_in(...)` - 入库
- `stock_out(...)` - 出库
- `get_records(...)` - 查询记录
- `get_recent(limit)` - 最近记录
- `cancel(...)` - 撤销记录

### inventory.py
库存管理，包含InventoryManager类，主要方法：
- `get_summary()` - 库存汇总
- `check(...)` - 库存盘点
- `adjust(...)` - 库存调整

预警阈值：篮球10、排球10、羽毛球拍20、乒乓球拍20、网球拍10

### report.py
报表统计，包含ReportManager类，主要方法：
- `get_dashboard_data()` - 仪表盘数据
- `get_statistics_by_date(start, end)` - 日期统计
- `export_to_csv(...)` - 导出CSV

### ui.py
PyQt5图形界面，包含：
- 登录对话框
- 主窗口（菜单栏、工具栏、标签页）
- 数据面板（支持点击查看详情）
- 器材、库存、出入库、用户管理等界面

### main.py
系统入口，初始化日志、数据库，显示登录界面，登录成功后显示主窗口。

## 数据库表

### users（用户表）
- id, username, password, real_name, role, email, phone
- created_at, updated_at, last_login, status

### equipment（器材表）
- id, equipment_code, name, type, specification
- quantity, unit, purchase_date, supplier, purchase_price
- status, remarks, created_at, updated_at, created_by

### in_out_record（出入库记录表）
- id, record_no, equipment_id, equipment_code
- equipment_name, equipment_type, record_type, quantity
- before_quantity, after_quantity, operator_id, operator_name
- operate_time, purpose, remarks

### inventory_warning（预警表）
- id, equipment_id, equipment_code, equipment_name
- equipment_type, current_quantity, warning_quantity
- warning_level, is_processed, created_at, processed_at, processed_by

### operation_log（操作日志表）
- id, user_id, username, operation_type, operation_module
- operation_desc, ip_address, operate_time, status

### inventory_check（盘点表）
- id, check_no, equipment_id, equipment_code, equipment_name
- system_quantity, actual_quantity, difference, check_type
- checker_id, checker_name, check_time, remarks

## 关键功能

1. 数据面板数字可点击查看详情
2. 出入库使用事务处理
3. 密码使用SHA256哈希存储

## 依赖

- PyQt5 >= 5.15.0
- pymysql >= 1.0.0 (可选)

## 运行环境

- Python 3.8+
- Windows / Linux / macOS

## 启动

```bash
python main.py
```

默认账号：admin / admin123
