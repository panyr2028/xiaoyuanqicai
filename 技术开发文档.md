# 校园体育器材出入库管理系统 技术开发文档

## 文档信息

| 项目名称 | 校园体育器材出入库管理系统 |
|---------|------------------------|
| 版本号 | V1.0.0 |
| 编制单位 | 校园体育器材管理部 |
| 编制日期 | 2026年5月 |

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构设计](#2-系统架构设计)
3. [数据库设计](#3-数据库设计)
4. [模块划分](#4-模块划分)
5. [关键算法说明](#5-关键算法说明)
6. [安全性设计](#6-安全性设计)
7. [错误处理机制](#7-错误处理机制)
8. [附录](#8-附录)

---

## 1. 系统概述

### 1.1 项目背景

校园体育器材管理是学校日常管理工作的重要组成部分。传统的纸质或Excel表格管理方式存在效率低、易出错、难以追溯等问题。本系统旨在通过信息化手段，实现对体育器材的规范化、流程化管理，提高管理效率，减少人为错误。

### 1.2 项目目标

本系统针对篮球、排球、羽毛球拍、乒乓球拍、网球拍五类常用体育器材，提供完整的出入库管理功能，实现：

- 器材信息的电子化管理
- 规范化的出入库流程
- 实时的库存监控与预警
- 完善的数据统计与报表功能
- 安全的用户权限管理

### 1.3 技术选型

| 类别 | 选用技术 | 说明 |
|-----|---------|-----|
| 开发语言 | Python 3.8+ | 高效、简洁、易维护 |
| GUI框架 | PyQt5 | 功能丰富、跨平台 |
| 数据库 | SQLite/MySQL | 灵活切换、便于部署 |
| 代码规范 | PEP 8 | 标准化代码风格 |

---

## 2. 系统架构设计

### 2.1 总体架构

本系统采用经典的三层架构设计：

```
+------------------+
|   表示层 (UI)    |  PyQt5 图形界面
+------------------+
        |
+------------------+
|   业务逻辑层     |  业务模块
+------------------+
        |
+------------------+
|   数据访问层     |  Database模块
+------------------+
        |
+------------------+
|   数据存储层     |  SQLite/MySQL
+------------------+
```

### 2.2 模块结构

```
sports_equipment_system/
├── config.py           # 系统配置模块
├── database.py         # 数据库连接与初始化
├── logger.py           # 日志管理模块
├── auth.py             # 用户认证与权限管理
├── equipment.py        # 器材信息管理
├── inout.py            # 出入库管理
├── inventory.py        # 库存管理
├── report.py           # 报表统计
├── ui.py               # PyQt5用户界面
├── main.py             # 系统入口
└── sports_equipment.db # SQLite数据库文件
```

### 2.3 配置文件设计

系统配置采用集中式管理，配置文件（config.py）包含以下配置项：

- **数据库配置**：支持MySQL和SQLite两种模式
- **日志配置**：日志级别、格式、文件路径、轮转策略
- **预警配置**：各类器材的最低库存阈值
- **备份配置**：自动备份策略、保留份数

配置设计遵循以下原则：
- 环境差异化配置独立管理
- 敏感信息不硬编码
- 配置项集中、分类清晰

---

## 3. 数据库设计

### 3.1 ER图

系统涉及的核心实体及其关系：

```
用户 (users)
  ├── id (PK)
  ├── username (UNIQUE)
  ├── password (hash)
  ├── role
  └── ...

器材 (equipment)
  ├── id (PK)
  ├── equipment_code (UNIQUE)
  ├── name, type, specification
  ├── quantity
  └── created_by (FK)

出入库记录 (in_out_record)
  ├── id (PK)
  ├── record_no (UNIQUE)
  ├── equipment_id (FK)
  ├── record_type (in/out/adjust)
  ├── quantity
  └── operator_id (FK)

库存预警 (inventory_warning)
  ├── id (PK)
  ├── equipment_id (FK)
  └── is_processed

库存盘点 (inventory_check)
  ├── id (PK)
  ├── equipment_id (FK)
  └── ...

操作日志 (operation_log)
  ├── id (PK)
  ├── user_id (FK)
  └── operation_type
```

### 3.2 数据表结构

#### 3.2.1 用户表 (users)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 用户ID |
| username | VARCHAR(50) | UNIQUE NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码（SHA256哈希） |
| real_name | VARCHAR(50) | | 真实姓名 |
| role | VARCHAR(20) | NOT NULL | 角色：admin/operator |
| email | VARCHAR(100) | | 邮箱 |
| phone | VARCHAR(20) | | 联系电话 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| last_login | TIMESTAMP | | 最后登录时间 |
| status | INTEGER | DEFAULT 1 | 状态：1启用/0禁用 |

#### 3.2.2 器材信息表 (equipment)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 器材ID |
| equipment_code | VARCHAR(50) | UNIQUE NOT NULL | 器材编号 |
| name | VARCHAR(100) | NOT NULL | 器材名称 |
| type | VARCHAR(50) | NOT NULL | 器材类型 |
| specification | VARCHAR(200) | | 规格型号 |
| quantity | INTEGER | DEFAULT 0 | 当前库存数量 |
| unit | VARCHAR(20) | DEFAULT '个' | 计量单位 |
| purchase_date | DATE | | 采购日期 |
| supplier | VARCHAR(200) | | 供应商 |
| purchase_price | DECIMAL(10,2) | | 采购价格 |
| status | INTEGER | DEFAULT 1 | 状态：1正常/0停用 |
| remarks | TEXT | | 备注 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |
| created_by | INTEGER | FK | 创建人 |

器材类型(type)枚举值：
- `basketball` - 篮球
- `volleyball` - 排球
- `badminton_racket` - 羽毛球拍
- `pingpong_racket` - 乒乓球拍
- `tennis_racket` - 网球拍

#### 3.2.3 出入库记录表 (in_out_record)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 记录ID |
| record_no | VARCHAR(50) | UNIQUE NOT NULL | 记录编号 |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| equipment_type | VARCHAR(50) | NOT NULL | 器材类型 |
| record_type | VARCHAR(20) | NOT NULL | 记录类型：in/out/adjust |
| quantity | INTEGER | NOT NULL | 数量变化 |
| before_quantity | INTEGER | NOT NULL | 操作前库存 |
| after_quantity | INTEGER | NOT NULL | 操作后库存 |
| operator_id | INTEGER | FK NOT NULL | 操作人 |
| operator_name | VARCHAR(50) | | 操作人姓名 |
| operate_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 操作时间 |
| purpose | VARCHAR(500) | | 用途说明 |
| remarks | TEXT | | 备注 |

#### 3.2.4 库存预警表 (inventory_warning)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 预警ID |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| equipment_type | VARCHAR(50) | NOT NULL | 器材类型 |
| current_quantity | INTEGER | NOT NULL | 当前库存 |
| warning_quantity | INTEGER | NOT NULL | 预警阈值 |
| warning_level | VARCHAR(20) | | 预警级别 |
| is_processed | INTEGER | DEFAULT 0 | 处理状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| processed_at | TIMESTAMP | | 处理时间 |
| processed_by | INTEGER | FK | 处理人 |

#### 3.2.5 操作日志表 (operation_log)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 日志ID |
| user_id | INTEGER | FK | 用户ID |
| username | VARCHAR(50) | | 用户名 |
| operation_type | VARCHAR(50) | NOT NULL | 操作类型 |
| operation_module | VARCHAR(50) | | 操作模块 |
| operation_desc | TEXT | | 操作描述 |
| ip_address | VARCHAR(50) | | IP地址 |
| operate_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 操作时间 |
| status | INTEGER | DEFAULT 1 | 状态 |

#### 3.2.6 库存盘点表 (inventory_check)

| 字段名 | 数据类型 | 约束 | 说明 |
|-------|---------|------|-----|
| id | INTEGER | PRIMARY KEY | 盘点ID |
| check_no | VARCHAR(50) | UNIQUE NOT NULL | 盘点编号 |
| equipment_id | INTEGER | FK NOT NULL | 器材ID |
| equipment_code | VARCHAR(50) | NOT NULL | 器材编号 |
| equipment_name | VARCHAR(100) | NOT NULL | 器材名称 |
| system_quantity | INTEGER | NOT NULL | 系统库存 |
| actual_quantity | INTEGER | NOT NULL | 实际库存 |
| difference | INTEGER | NOT NULL | 差异 |
| check_type | VARCHAR(20) | | 盘点类型 |
| checker_id | INTEGER | FK NOT NULL | 盘点人 |
| checker_name | VARCHAR(50) | | 盘点人姓名 |
| check_time | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 盘点时间 |
| remarks | TEXT | | 备注 |

### 3.3 索引设计

为提高查询效率，在以下字段上建立索引：

- `equipment(equipment_code)` - UNIQUE INDEX
- `equipment(type)` - INDEX
- `in_out_record(equipment_id, operate_time)` - COMPOSITE INDEX
- `in_out_record(record_type, operate_time)` - COMPOSITE INDEX
- `operation_log(user_id, operate_time)` - COMPOSITE INDEX

---

## 4. 模块划分

### 4.1 config.py - 配置模块

**功能说明**：系统配置集中管理

**主要功能**：
- 数据库连接配置
- 日志系统配置
- 预警阈值配置
- 器材类型定义
- 用户角色权限配置

**关键变量**：
```python
APP_NAME = "校园体育器材出入库管理系统"
APP_VERSION = "1.0.0"
USE_SQLITE = True  # 是否使用SQLite数据库
SQLITE_DB_PATH = 'sports_equipment.db'
INVENTORY_WARNING_CONFIG = {
    'basketball': 10,
    'volleyball': 10,
    'badminton_racket': 20,
    'pingpong_racket': 20,
    'tennis_racket': 10
}
EQUIPMENT_TYPES = [
    ('basketball', '篮球'),
    ('volleyball', '排球'),
    ('badminton_racket', '羽毛球拍'),
    ('pingpong_racket', '乒乓球拍'),
    ('tennis_racket', '网球拍')
]
```

### 4.2 database.py - 数据库模块

**功能说明**：数据库连接管理与初始化

**主要功能**：
- 单例模式数据库连接管理
- 支持SQLite和MySQL切换
- 自动初始化数据库表结构
- 创建默认管理员账户
- 数据库备份与恢复

**关键类**：
```python
class Database:
    def execute(sql, params):     # 执行SQL
    def fetchall(sql, params):    # 查询所有
    def fetchone(sql, params):    # 查询单条
    def backup_database():        # 备份数据库
    def close():                  # 关闭连接
```

**初始化流程**：
1. 建立数据库连接
2. 创建所有数据表（IF NOT EXISTS）
3. 创建默认管理员账户（admin/admin123）

### 4.3 auth.py - 认证模块

**功能说明**：用户认证与权限管理

**主要功能**：
- 用户登录/登出
- 密码哈希与验证（SHA256）
- 用户注册
- 密码修改与重置
- 权限检查

**关键类**：
```python
class UserAuth:
    @staticmethod
    def login(username, password):           # 用户登录
    @staticmethod
    def verify_password(password, hash):     # 密码验证
    @staticmethod
    def register(username, password, ...):   # 用户注册
    @staticmethod
    def change_password(user_id, old, new):  # 修改密码
    @staticmethod
    def reset_password(user_id, new_pwd, admin_id):  # 重置密码

class Permission:
    @staticmethod
    def check_permission(role, permission):  # 权限检查

class UserManager:
    @staticmethod
    def get_all_users(page, page_size, ...): # 获取用户列表
    @staticmethod
    def update_user(user_id, ...):           # 更新用户
    @staticmethod
    def delete_user(user_id, current_user_id): # 删除用户
```

**权限模型**：

| 角色 | 权限范围 |
|-----|---------|
| admin | 所有权限 |
| operator | 器材查看/添加/编辑、库存查看、出入库记录、报表查看/导出 |

### 4.4 equipment.py - 器材管理模块

**功能说明**：器材基本信息管理

**主要功能**：
- 器材编号自动生成
- 器材信息CRUD操作
- 多条件器材查询
- 器材统计

**器材编号规则**：
- 篮球：BB + 日期(YYYYMMDD) + 序号(4位)
- 排球：VB + 日期(YYYYMMDD) + 序号(4位)
- 羽毛球拍：BR + 日期(YYYYMMDD) + 序号(4位)
- 乒乓球拍：PR + 日期(YYYYMMDD) + 序号(4位)
- 网球拍：TR + 日期(YYYYMMDD) + 序号(4位)

示例：`BB202605040001` 表示2026年5月4日入库的第1个篮球

**关键函数**：
```python
class EquipmentManager:
    @staticmethod
    def generate_equipment_code(type):       # 生成器材编号
    @staticmethod
    def add_equipment(...):                  # 添加器材
    @staticmethod
    def update_equipment(eq_id, ...):        # 更新器材
    @staticmethod
    def delete_equipment(eq_id, user_id):    # 删除器材
    @staticmethod
    def get_all_equipments(page, ...):       # 分页查询器材
    @staticmethod
    def get_equipment_statistics():          # 获取统计
    @staticmethod
    def get_specification_details_by_type(equipment_type): # 获取规格详情
```

### 4.5 inout.py - 出入库管理模块

**功能说明**：器材入库、出库操作管理

**主要功能**：
- 入库登记（增加库存）
- 出库登记（减少库存）
- 库存变更自动记录
- 记录撤销功能
- 多条件记录查询

**出入库流程**：

入库流程：
```
1. 选择器材 → 2. 输入数量 → 3. 填写用途 → 4. 确认入库
                                              ↓
                                      记录 in_out_record
                                      更新 equipment.quantity
```

出库流程：
```
1. 选择器材 → 2. 输入数量 → 3. 检查库存 → 4. 填写用途 → 5. 确认出库
                                                                 ↓
                                                         记录 in_out_record
                                                         更新 equipment.quantity
```

**记录编号规则**：
`IO` + 日期(YYYYMMDD) + 时间(HHMMSS)
示例：`IO20260504143025`

**关键函数**：
```python
class InOutManager:
    @staticmethod
    def stock_in(eq_id, qty, purpose, ...):    # 入库
    @staticmethod
    def stock_out(eq_id, qty, purpose, ...):   # 出库
    @staticmethod
    def get_records(page, ...):               # 查询记录
    @staticmethod
    def cancel_record(record_id, ...):         # 撤销记录
    @staticmethod
    def get_record_statistics(...):            # 获取统计
    @staticmethod
    def get_recent_records(limit=10):          # 获取最近记录
```

### 4.6 inventory.py - 库存管理模块

**功能说明**：库存监控与调整

**主要功能**：
- 库存汇总查询
- 库存预警检测与生成
- 库存盘点
- 库存调整

**预警规则**：
| 器材类型 | 最低库存阈值 |
|---------|------------|
| 篮球 | 10 |
| 排球 | 10 |
| 羽毛球拍 | 20 |
| 乒乓球拍 | 20 |
| 网球拍 | 10 |

**预警级别**：
- `warning`：当前库存 < 预警阈值
- `critical`：当前库存 = 0

**关键函数**：
```python
class InventoryManager:
    @staticmethod
    def get_inventory_summary():              # 库存汇总
    @staticmethod
    def get_low_stock_warnings():             # 获取预警
    @staticmethod
    def check_and_create_warnings():          # 检测并创建预警
    @staticmethod
    def inventory_check(eq_id, actual_qty, ...): # 库存盘点
    @staticmethod
    def inventory_adjustment(eq_id, new_qty, ...): # 库存调整
```

### 4.7 report.py - 报表统计模块

**功能说明**：数据统计与报表生成

**主要功能**：
- 仪表盘数据获取
- 多条件综合查询
- 日报/月报生成
- 数据导出CSV

**关键函数**：
```python
class ReportManager:
    @staticmethod
    def get_dashboard_data():                 # 仪表盘数据
    @staticmethod
    def query_equipment(...):                 # 查询器材
    @staticmethod
    def query_records(...):                   # 查询记录
    @staticmethod
    def generate_daily_report(date=None):     # 生成日报
    @staticmethod
    def generate_monthly_report(year, month): # 生成月报
    @staticmethod
    def export_to_csv(data, filename, headers): # 导出CSV
    @staticmethod
    def get_statistics_by_date(start, end):   # 日期统计
```

### 4.8 logger.py - 日志模块

**功能说明**：日志记录与管理

**主要功能**：
- 分模块日志记录
- 日志文件轮转
- 操作日志数据库记录
- 日志级别管理

**日志格式**：
```
2026-05-04 14:30:25 - module_name - INFO - message
```

**日志文件**：
- 路径：`logs/system.log`
- 轮转大小：10MB
- 保留份数：5份

**关键函数**：
```python
class Logger:
    @classmethod
    def get_logger(cls, name='SportsEquipment'):  # 获取日志实例

def log_operation(user_id, username, op_type, module, desc, ip=None):
    """记录操作日志到数据库"""

def log_info(message, module='System'):    # 记录信息日志
def log_error(message, module='System'):   # 记录错误日志
def log_warning(message, module='System'): # 记录警告日志
```

### 4.9 ui.py - 用户界面模块

**功能说明**：PyQt5图形用户界面

**主要功能**：
- 登录界面
- 主窗口布局
- 数据面板（仪表盘）
- 器材管理界面
- 出入库管理界面
- 库存管理界面
- 用户管理界面
- 报表统计界面

**界面特点**：
- 标签页式导航
- 响应式布局设计
- 数据面板数字支持点击查看规格详情
- 统一的表格样式和字体设置

---

## 5. 关键算法说明

### 5.1 器材编号生成算法

```python
def generate_equipment_code(equipment_type):
    # 1. 获取类型前缀
    prefix_map = {'basketball': 'BB', 'volleyball': 'VB', ...}
    prefix = prefix_map[equipment_type]

    # 2. 获取当天日期
    today = datetime.now().strftime('%Y%m%d')

    # 3. 查询当天最大序号
    sql = "SELECT equipment_code FROM equipment WHERE equipment_code LIKE ? ORDER BY id DESC LIMIT 1"
    result = db.fetchone(sql, (f"{prefix}{today}%",))

    # 4. 计算新序号
    if result:
        last_seq = int(result['equipment_code'][-4:])
        new_seq = last_seq + 1
    else:
        new_seq = 1

    # 5. 组合编号
    return f"{prefix}{today}{new_seq:04d}"
```

**时间复杂度**：O(1)
**空间复杂度**：O(1)

### 5.2 库存预警检测算法

```python
def check_and_create_warnings():
    warnings = []

    # 1. 遍历所有预警配置
    for equip_type, threshold in INVENTORY_WARNING_CONFIG.items():
        # 2. 查询低于阈值的器材
        sql = "SELECT * FROM equipment WHERE type = ? AND status = 1 AND quantity < ?"
        results = db.fetchall(sql, (equip_type, threshold))

        # 3. 为每个低库存器材创建预警
        for eq in results:
            # 4. 检查是否已存在未处理预警
            if not db.fetchone(check_sql, (eq['id'],)):
                # 5. 创建新预警
                db.execute(insert_sql, (...))

    return len(warnings)
```

### 5.3 密码哈希算法

采用SHA256哈希算法，不使用明文存储密码：

```python
import hashlib

def hash_password(password):
    return hashlib.sha256(password.encode()).hexdigest()

def verify_password(password, password_hash):
    return hash_password(password) == password_hash
```

### 5.4 出入库事务处理算法

```python
def stock_in(equipment_id, quantity, ...):
    try:
        # 1. 开启事务
        with db.get_cursor() as cursor:
            # 2. 获取器材当前库存
            equipment = get_equipment_by_id(equipment_id)
            before_qty = equipment['quantity']

            # 3. 插入出入库记录
            cursor.execute(insert_record_sql, (...))

            # 4. 更新器材库存
            new_qty = before_qty + quantity
            cursor.execute(update_equipment_sql, (new_qty, equipment_id))

        # 5. 自动提交
        return True, "入库成功"

    except Exception as e:
        # 6. 异常时自动回滚
        db.rollback()
        return False, str(e)
```

---

## 6. 安全性设计

### 6.1 认证机制

- 用户登录验证
- 密码SHA256哈希存储
- 登录失败锁定（预留接口）
- 会话管理（预留接口）

### 6.2 权限控制

基于角色的访问控制（RBAC）：

| 功能模块 | admin | operator |
|---------|-------|---------|
| 用户管理 | ✓ | ✗ |
| 器材管理-查看 | ✓ | ✓ |
| 器材管理-增删改 | ✓ | ✓ |
| 出入库登记 | ✓ | ✓ |
| 库存盘点 | ✓ | ✗ |
| 库存调整 | ✓ | ✗ |
| 报表查看 | ✓ | ✓ |
| 数据备份 | ✓ | ✗ |

### 6.3 数据安全

- 数据库连接参数可配置
- 支持MySQL和SQLite切换
- 定期自动备份
- 操作日志完整记录
- 敏感操作二次确认

### 6.4 日志安全

所有关键操作均记录日志：
- 用户登录/登出
- 器材增删改
- 出入库操作
- 库存调整
- 系统备份

---

## 7. 错误处理机制

### 7.1 分层错误处理

```
UI层 → 业务逻辑层 → 数据访问层
  ↓           ↓            ↓
QMessageBox  返回错误元组  数据库异常
```

### 7.2 错误返回模式

业务方法统一返回 `(bool, str)` 元组：

```python
def some_operation(...):
    try:
        # 业务逻辑
        return True, "操作成功"
    except Exception as e:
        log_error(f"操作失败: {e}")
        return False, f"操作失败: {str(e)}"
```

### 7.3 数据库事务处理

```python
with db.get_cursor() as cursor:
    try:
        cursor.execute(sql1, params1)
        cursor.execute(sql2, params2)
        # 无异常自动提交
    except:
        # 异常时自动回滚
        raise
```

---

## 8. 附录

### 8.1 项目文件清单

| 文件名 | 说明 | 功能 |
|-------|------|-----|
| config.py | 系统配置 | 数据库、日志、预警等配置项 |
| database.py | 数据库模块 | 数据库连接、初始化、备份 |
| logger.py | 日志模块 | 日志记录与管理 |
| auth.py | 认证模块 | 用户登录、权限管理 |
| equipment.py | 器材管理 | 器材CRUD、统计 |
| inout.py | 出入库管理 | 入库、出库、记录管理 |
| inventory.py | 库存管理 | 库存预警、盘点、调整 |
| report.py | 报表统计 | 数据统计、报表导出 |
| ui.py | 用户界面 | PyQt5图形界面 |
| main.py | 入口文件 | 系统启动 |

### 8.2 系统依赖

```
PyQt5 >= 5.15.0
pymysql >= 1.0.0  (可选，用于MySQL)
```

### 8.3 运行环境

- Python 3.8+
- Windows / Linux / macOS
- 内存：最低 4GB
- 硬盘：最低 100MB 可用空间

### 8.4 数据面板交互功能

数据面板中的数字支持点击交互：
- 点击"资产总数"数字：显示该类型器材各规格的资产总数明细
- 点击"库存数量"数字：显示该类型器材各规格的库存数量明细
- 鼠标悬停显示下划线提示可点击

---

## 文档版本

| 版本 | 日期 | 修改说明 |
|-----|------|---------|
| 1.0.0 | 2026-05-05 | 初始版本发布，包含完整功能说明 |

---

*本文档为校园体育器材出入库管理系统技术开发文档，版权所有*