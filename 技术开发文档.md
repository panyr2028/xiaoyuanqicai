# 技术开发文档

## 项目概述

校园体育器材出入库管理系统用于管理篮球、排球、羽毛球拍、乒乓球拍、网球拍等器材的出入库业务。

## 技术架构

### 技术选型

- Python 3.8+
- PyQt5（图形界面）
- SQLite/MySQL（数据库）
- PEP 8（代码规范）

### 系统架构

三层架构：
- UI层：PyQt5图形界面
- 业务层：业务模块
- 数据层：Database模块 + SQLite/MySQL

### 模块结构

```
xiaoyuanqicai/
├── config.py           # 配置
├── database.py         # 数据库
├── logger.py           # 日志
├── auth.py             # 认证
├── equipment.py        # 器材
├── inout.py            # 出入库
├── inventory.py        # 库存
├── report.py           # 报表
├── ui.py               # 界面
├── main.py             # 入口
└── sports_equipment.db # 数据库
```

## 数据库设计

### 实体关系

```
用户 (users) ← 操作 → 出入库记录 (in_out_record)
器材 (equipment) ← 关联 → 出入库记录
器材 ← 预警 → 库存预警 (inventory_warning)
器材 ← 盘点 → 库存盘点 (inventory_check)
用户 ← 日志 → 操作日志 (operation_log)
```

### 数据表

#### users（用户表）
id, username, password(hash), real_name, role(admin/operator), email, phone, created_at, updated_at, last_login, status

#### equipment（器材表）
id, equipment_code, name, type, specification, quantity, unit, purchase_date, supplier, purchase_price, status, remarks, created_at, updated_at, created_by

器材类型：basketball(篮球), volleyball(排球), badminton_racket(羽毛球拍), pingpong_racket(乒乓球拍), tennis_racket(网球拍)

#### in_out_record（出入库记录表）
id, record_no, equipment_id, equipment_code, equipment_name, equipment_type, record_type(in/out), quantity, before_quantity, after_quantity, operator_id, operator_name, operate_time, purpose, remarks

#### inventory_warning（预警表）
id, equipment_id, equipment_code, equipment_name, equipment_type, current_quantity, warning_quantity, warning_level, is_processed, created_at, processed_at, processed_by

#### operation_log（操作日志表）
id, user_id, username, operation_type, operation_module, operation_desc, ip_address, operate_time, status

#### inventory_check（盘点表）
id, check_no, equipment_id, equipment_code, equipment_name, system_quantity, actual_quantity, difference, check_type, checker_id, checker_name, check_time, remarks

### 索引

- equipment(equipment_code) - UNIQUE
- equipment(type) - INDEX
- in_out_record(equipment_id, operate_time) - COMPOSITE
- in_out_record(record_type, operate_time) - COMPOSITE
- operation_log(user_id, operate_time) - COMPOSITE

## 模块说明

### config.py
系统配置：APP_NAME, APP_VERSION, USE_SQLITE, SQLITE_DB_PATH, DB_CONFIG, LOG_CONFIG, INVENTORY_WARNING_CONFIG, EQUIPMENT_TYPES, USER_ROLES

### database.py
Database类：
- execute(sql, params) - 执行SQL
- fetchall(sql, params) - 查询多条
- fetchone(sql, params) - 查询单条
- backup_database() - 备份
- close() - 关闭

函数：init_database(), get_db()

### auth.py

UserAuth类：
- login(username, password)
- hash_password(password) - SHA256
- verify_password(password, hash)
- register(username, password, real_name, role, email, phone)
- change_password(user_id, old, new)
- reset_password(user_id, new_pwd, admin_id)

Permission类：
- check_permission(role, permission)

UserManager类：
- get_user_by_id(user_id)
- get_by_id(user_id) - 别名
- get_all_users(page, page_size, role, keyword)
- get_all(page, page_size, role, keyword) - 别名
- update_user(user_id, real_name, email, phone, role, status)
- delete_user(user_id, current_user_id)

### equipment.py

EquipmentManager类：
- generate_equipment_code(type) - BB/YYMMDD/NNNN
- generate_code(type) - 别名
- add(...) - 添加器材
- add_equipment(...) - 别名
- update(...) - 更新器材
- update_equipment(...) - 别名
- delete(...) - 删除器材
- delete_equipment(...) - 别名
- get_by_id(id) - 获取器材
- get_equipment_by_id(id) - 别名
- get_all(...) - 查询列表
- get_all_equipments(...) - 别名
- get_equipment_statistics() - 统计
- update_quantity(eq_id, qty, op_id, op_name) - 更新数量
- get_specification_details_by_type(type) - 规格详情

编号规则：类型前缀 + 日期 + 序号
示例：BB202605040001

### inout.py

InOutManager类：
- generate_record_no() - IO + 日期时间
- generate_no() - 别名
- stock_in(eq_id, qty, purpose, ...) - 入库
- stock_out(eq_id, qty, purpose, ...) - 出库
- get_records(page, ...) - 查询
- get_by_id(id) - 获取记录
- get_record_by_id(id) - 别名
- get_recent(limit) - 最近记录
- get_recent_records(limit) - 别名
- cancel(id, op_id, op_name, reason) - 撤销
- cancel_record(id, op_id, op_name, reason) - 别名
- get_record_statistics(start, end, type) - 统计

### inventory.py

InventoryManager类：
- get_summary() - 库存汇总
- get_inventory_summary() - 别名
- get_low_stock_warnings() - 预警列表
- check_and_create_warnings() - 检测预警
- get_warnings(page, size, is_processed) - 查询预警
- process_warning(warning_id, op_id) - 处理预警
- check(eq_id, actual_qty, type, checker_id, checker_name, remarks) - 盘点
- inventory_check(...) - 别名
- adjust(eq_id, new_qty, reason, op_id, op_name) - 调整
- inventory_adjustment(...) - 别名

预警阈值：篮球10、排球10、羽毛球拍20、乒乓球拍20、网球拍10
预警级别：warning(低于阈值)、critical(为零)

### report.py

ReportManager类：
- get_dashboard_data() - 仪表盘
- query_equipment(type, keyword, status) - 查询器材
- query_records(type, rec_type, start, end, keyword) - 查询记录
- generate_daily_report(date) - 日报
- generate_monthly_report(year, month) - 月报
- export_to_csv(data, filename, headers) - 导出
- get_statistics_by_date(start, end) - 日期统计

### logger.py

Logger类：
- get_logger(name='SportsEquipment') - 获取日志实例

函数：
- log_operation(user_id, username, op_type, module, desc, ip) - 操作日志
- log_info(message, module) - 信息日志
- log_error(message, module) - 错误日志
- log_warning(message, module) - 警告日志
- get_operation_logs(user_id, op_type, start, end, limit) - 查询日志

日志格式：日期时间 - 模块 - 级别 - 消息
日志文件：logs/system.log，10MB轮转，保留5份

### ui.py
PyQt5图形界面，包含：
- 登录对话框
- 主窗口（菜单栏、工具栏、标签页）
- 数据面板（支持点击查看详情）
- 器材、库存、出入库、用户管理等界面

### main.py
入口文件：
1. 初始化日志
2. 初始化数据库
3. 显示登录界面
4. 登录成功显示主窗口

## 关键算法

### 器材编号生成
1. 获取类型前缀（basketball→BB）
2. 获取当天日期
3. 查询当天最大序号
4. 序号+1
5. 组合：前缀+日期+4位序号

### 密码哈希
使用SHA256：
```python
hashlib.sha256(password.encode()).hexdigest()
```

### 出入库事务
1. 获取当前库存
2. 插入记录
3. 更新库存
4. 自动提交/回滚

## 安全设计

### 认证
- 密码SHA256哈希
- 登录验证

### 权限（RBAC）

| 功能 | admin | operator |
|-----|-------|---------|
| 用户管理 | ✓ | ✗ |
| 器材增删改 | ✓ | ✓ |
| 出入库 | ✓ | ✓ |
| 库存盘点 | ✓ | ✗ |
| 库存调整 | ✓ | ✗ |
| 报表 | ✓ | ✓ |
| 备份 | ✓ | ✗ |

### 数据安全
- 操作日志记录
- 自动备份
- 敏感操作确认

## 错误处理

业务方法返回(bool, str)元组：
```python
try:
    # 业务逻辑
    return True, "成功"
except Exception as e:
    return False, str(e)
```

## 依赖

- PyQt5 >= 5.15.0
- pymysql >= 1.0.0 (可选)

## 运行环境

- Python 3.8+
- Windows / Linux / macOS

版本：V1.0.0 (2026-05-05)
